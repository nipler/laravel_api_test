# Laravel Document api

# Установка

  - Клонировать репозиторий
     ```sh
    git clone https://github.com/nipler/laravel_api_test.git
    ```
  - Запустить composer
     ```sh
    composer install
    ```
  - Выполнить миграции
     ```sh
    php artisan migrate
    ```
